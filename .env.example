# Local PostgreSQL dev stack. These demo credentials match compose.dev.yml.
THOTH_POSTGRES_DSN=postgresql://thoth:thoth_dev@localhost:5432/thoth
THOTH_TEST_POSTGRES_DSN=postgresql://thoth:thoth_dev@localhost:5432/thoth_test

# Override only if port 5432 is already in use locally.
THOTH_POSTGRES_PORT=5432
