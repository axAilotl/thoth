# OpenAPI reference

The HTTP API is a reference transport for signed producer batches, delta packs, Blob ranges, reads, authorization, and mindpack export. CCF local operation and file/USB pack transfer do not depend on HTTP.
