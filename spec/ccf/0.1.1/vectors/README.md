# CCF 0.1.1 test vectors

These vectors exercise canonical JSON, compartment commitments, portable object hashes, submission hashes, signed producer batches, Merkle roots, commit signatures, semantic-catalog hashing, and numeric admission ordering.

The private keys in this directory are deterministic **test material only**. They MUST NOT be used for a real archive or device.

Run:

```bash
node tools/verify-vectors.mjs
```
